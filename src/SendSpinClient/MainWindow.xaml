<Window x:Class="SendSpinClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SendSpinClient"
        xmlns:vm="clr-namespace:SendSpinClient.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="SendSpin Client"
        Icon="Resources/Icons/sendspinDesktop.png"
        Height="500" Width="380"
        MinHeight="400" MinWidth="320"
        Background="{StaticResource BackgroundDarkBrush}"
        WindowStyle="SingleBorderWindow">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundBrush}" Padding="16">
            <StackPanel>
                <TextBlock Text="SendSpin" Style="{StaticResource HeaderText}"/>
                <TextBlock Style="{StaticResource CaptionText}" Margin="0,4,0,0">
                    <Run Text="Windows Client"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Status Area -->
        <Border Grid.Row="1" Margin="16,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Status Message -->
                <Border Grid.Row="0"
                        Background="{StaticResource SurfaceBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel>
                        <!-- Hosting Status Icon -->
                        <Border HorizontalAlignment="Center"
                                Width="64" Height="64"
                                CornerRadius="32"
                                Margin="0,0,0,12">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="28">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="&#x1F4E1;"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Text" Value="&#x1F3B5;"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <!-- Status Text -->
                        <TextBlock Text="{Binding StatusMessage}"
                                   Style="{StaticResource BodyText}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"/>

                        <!-- Connected Server Name -->
                        <TextBlock Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}"
                                   Margin="0,8,0,0"
                                   Style="{StaticResource SubHeaderText}"
                                   TextAlignment="Center">
                            <Run Text="Connected to: "/>
                            <Run Text="{Binding ConnectedServerName, Mode=OneWay}" FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Connected Servers List (when multiple) -->
                <Border Grid.Row="1"
                        Margin="0,8,0,0"
                        Visibility="{Binding ConnectedServers.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Connected Servers"
                                   Style="{StaticResource CaptionText}"
                                   Margin="0,0,0,4"/>
                        <ItemsControl ItemsSource="{Binding ConnectedServers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceBrush}"
                                            CornerRadius="4"
                                            Padding="8"
                                            Margin="0,2">
                                        <TextBlock Text="{Binding ServerName}"
                                                   Style="{StaticResource BodyText}"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Manual Connection Section (for cross-subnet scenarios) -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8"
                Padding="12"
                Margin="16,8"
                VerticalAlignment="Bottom"
                Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverse}">
            <StackPanel>
                <TextBlock Text="Manual Connection"
                           Style="{StaticResource CaptionText}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="For cross-subnet connections, enter the server URL directly:"
                           Style="{StaticResource BodyText}"
                           TextWrapping="Wrap"
                           Opacity="0.7"
                           Margin="0,0,0,8"
                           FontSize="11"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding ManualServerUrl, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8"
                             Background="{StaticResource BackgroundDarkBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource SurfaceLightBrush}"
                             BorderThickness="1"
                             VerticalContentAlignment="Center"
                             FontFamily="Consolas"
                             FontSize="11"/>
                    <Button Grid.Column="1"
                            Content="Connect"
                            Command="{Binding ConnectToServerCommand}"
                            IsEnabled="{Binding IsConnecting, Converter={StaticResource InverseBoolConverter}}"
                            Margin="8,0,0,0"
                            Padding="12,8"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
                <!-- Connecting indicator -->
                <TextBlock Text="Connecting..."
                           Style="{StaticResource CaptionText}"
                           Margin="0,8,0,0"
                           Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <!-- Now Playing Section -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                Padding="16"
                Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Album Art -->
                <Border Grid.Column="0"
                        Width="50" Height="50"
                        Background="{StaticResource SurfaceLightBrush}"
                        CornerRadius="4">
                    <Grid>
                        <!-- Placeholder icon when no artwork -->
                        <TextBlock Text="&#x1F3B5;"
                                   FontSize="24"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding AlbumArtwork, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=inverse}"/>
                        <!-- Actual artwork -->
                        <Image Stretch="UniformToFill"
                               Visibility="{Binding AlbumArtwork, Converter={StaticResource NullToVisibilityConverter}}">
                            <Image.Source>
                                <Binding Path="AlbumArtwork">
                                    <Binding.Converter>
                                        <local:ByteArrayToImageConverter/>
                                    </Binding.Converter>
                                </Binding>
                            </Image.Source>
                        </Image>
                    </Grid>
                </Border>

                <!-- Track Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentTrack.Title, FallbackValue='Not Playing'}"
                               Style="{StaticResource SubHeaderText}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding CurrentTrack.Artist, FallbackValue=''}"
                               Style="{StaticResource BodyText}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding CurrentCodec}"
                               Style="{StaticResource CaptionText}"
                               Opacity="0.6"
                               Margin="0,4,0,0"
                               Visibility="{Binding CurrentCodec, Converter={StaticResource NullToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Controls -->
        <Border Grid.Row="3" Background="{StaticResource BackgroundBrush}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Playback Controls -->
                <StackPanel Grid.Row="0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Button Content="&#x23EE;"
                            Style="{StaticResource IconButton}"
                            Command="{Binding PreviousTrackCommand}"
                            ToolTip="Previous"/>
                    <Button Content="{Binding IsPlaying, Converter={StaticResource PlayPauseIconConverter}}"
                            Style="{StaticResource IconButton}"
                            Width="64" Height="64"
                            FontSize="32"
                            Command="{Binding PlayPauseCommand}"
                            ToolTip="Play/Pause"
                            Margin="16,0"/>
                    <Button Content="&#x23ED;"
                            Style="{StaticResource IconButton}"
                            Command="{Binding NextTrackCommand}"
                            ToolTip="Next"/>
                </StackPanel>

                <!-- Volume Control -->
                <StackPanel Grid.Row="1"
                            Margin="0,8,0,0"
                            Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="{Binding IsMuted, Converter={StaticResource MuteIconConverter}}"
                                Style="{StaticResource IconButton}"
                                Width="32" Height="32"
                                FontSize="16"
                                Command="{Binding ToggleMuteCommand}"
                                ToolTip="Mute/Unmute"/>

                        <Slider Grid.Column="1"
                                Minimum="0" Maximum="100"
                                Value="{Binding Volume}"
                                VerticalAlignment="Center"
                                Margin="8,0"/>

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Volume, StringFormat={}{0}%}"
                                   Style="{StaticResource CaptionText}"
                                   VerticalAlignment="Center"
                                   Width="35"/>
                    </Grid>
                </StackPanel>

                <!-- Error Message -->
                <TextBlock Grid.Row="1"
                           Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorBrush}"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
