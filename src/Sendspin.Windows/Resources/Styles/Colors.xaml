<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Primary Colors (brand green, derived from the disc icon's gradient:
         #5BE58A light stop → #1F9D54 dark stop) -->
    <Color x:Key="PrimaryColor">#1F9D54</Color>
    <Color x:Key="PrimaryDarkColor">#177A41</Color>
    <Color x:Key="PrimaryLightColor">#5BE58A</Color>

    <!-- Accent Colors -->
    <Color x:Key="AccentColor">#06B6D4</Color>
    <Color x:Key="AccentDarkColor">#0891B2</Color>

    <!-- Background Colors (neutral dark slate - low chroma, no blue/purple cast,
         matching the Now Playing design. This is the "resting" backdrop; album-art
         reactive tinting is layered on top of it.) -->
    <Color x:Key="BackgroundDarkColor">#181B1D</Color>
    <Color x:Key="BackgroundColor">#21252A</Color>
    <Color x:Key="SurfaceColor">#2B3036</Color>
    <Color x:Key="SurfaceLightColor">#393F46</Color>

    <!-- Text Colors -->
    <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#A0A0B0</Color>
    <Color x:Key="TextMutedColor">#606070</Color>

    <!-- Player accent (active transport toggles: shuffle / repeat) -->
    <Color x:Key="PlayerAccentColor">#4ADE80</Color>

    <!-- State Colors -->
    <Color x:Key="SuccessColor">#10B981</Color>
    <Color x:Key="WarningColor">#F59E0B</Color>
    <Color x:Key="ErrorColor">#EF4444</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentDarkColor}"/>

    <SolidColorBrush x:Key="BackgroundDarkBrush" Color="{StaticResource BackgroundDarkColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>

    <SolidColorBrush x:Key="PlayerAccentBrush" Color="{StaticResource PlayerAccentColor}"/>

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

</ResourceDictionary>
