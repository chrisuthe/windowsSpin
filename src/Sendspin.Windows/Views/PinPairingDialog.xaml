<Window x:Class="Sendspin.Windows.Views.PinPairingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pairing PIN"
        Width="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Key Icon and Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#x1F511;" FontSize="24" Foreground="#F9E2AF" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Pairing PIN" FontSize="16" FontWeight="SemiBold" Foreground="#CDD6F4" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Explanation -->
        <TextBlock Grid.Row="1"
                   Text="A server is pairing with this player. Enter the PIN below into the server's pairing prompt (e.g. Music Assistant) to finish. The PIN belongs to this pairing attempt only; if the server retries, a new PIN appears here."
                   TextWrapping="Wrap"
                   Foreground="#A6ADC8"
                   FontSize="13"
                   LineHeight="20"
                   Margin="0,0,0,12"/>

        <!-- PIN (display only; the operator types it into the server) -->
        <Border Grid.Row="2"
                Background="#11111B"
                BorderBrush="#45475A"
                BorderThickness="1"
                Padding="10,16">
            <TextBlock x:Name="PinText"
                       FontFamily="Consolas"
                       FontSize="36"
                       FontWeight="SemiBold"
                       Foreground="#CDD6F4"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Notice (PIN replaced by a retry) -->
        <TextBlock Grid.Row="3"
                   x:Name="NoticeText"
                   TextWrapping="Wrap"
                   Foreground="#F9E2AF"
                   FontSize="12"
                   LineHeight="18"
                   Margin="0,8,0,0"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CloseButton"
                    Content="Close"
                    Click="CloseButton_Click"
                    Width="80"
                    Padding="12,8"
                    Background="Transparent"
                    Foreground="#A6ADC8"
                    BorderBrush="#45475A"
                    BorderThickness="1"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
